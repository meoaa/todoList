spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/todo
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop # 테스트 끝나면 삭제
    show-sql: true
    properties:
      hibernate:
        format_sql: true